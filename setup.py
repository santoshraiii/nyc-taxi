from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Machine learning project to predict the trip duration for taxi in NYC',
    author='Santosh rai',
    license='MIT',
)
